# Scratch space

This directory is disposable. Useful work must be promoted into `src`,
`evaluation`, or `tests` before it becomes part of KRISP-U.

Scratch code may not be imported by active source or stable evaluation code.
